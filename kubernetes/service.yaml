apiVersion: v1
kind: Service
metadata:
  name: kristian-web
  namespace: kristian-lab
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kristian-web
  ports:
    - name: http
      port: 80
      targetPort: http